<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="seg2105.uottawa.com.taskmanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SpecificTaskActivity"
            android:label="@string/title_activity_specific_task"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="seg2105.uottawa.com.taskmanager.MainActivity" />
        </activity>
        <activity android:name=".NewUserActivity" />
        <activity
            android:name=".EquipmentActivity"
            android:label="@string/title_equipment_activity"
            android:parentActivityName=".MainActivity" />
        <activity
            android:name=".ShoppingList"
            android:label="@string/title_activity_shopping_list"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="seg2105.uottawa.com.taskmanager.MainActivity" />
        </activity>
    </application>

</manifest>